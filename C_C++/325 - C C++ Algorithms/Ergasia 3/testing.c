  #include <stdio.h>
  #include <stdlib.h>
  #include <string.h>
  #include "Graph.h"
  #define TRUE 1
  #define  FALSE 0 
  
  void main(void){
  	GRAPH g; 
  	int i , j ; 
  	int num_of_countries; 
  	
	char *exit_string_for= "exit_string_for"; 
	char *arxiki_diadromi = "1"; 
  	
  	num_of_countries = graph_init_from_file(&g,"graph.graph");
  	ektipwsi_graphou(g);
  	ektypwsi_visit(g);
  	
 while(1){
    	//ua mpenei diadromi || tha epistrefi varos , diadromi	
  		epistrofi_diadromi = Diadromi(,,,,); 
  		
  		
  		//an i diadromi ine i mikroteri tha kratate 
  	else if( diadromi_epistrofis<  mikrotero_varos ; ){
  			mikrotero_varos = epistrofi_diadromi;
			mikroteri_diavromi =  epistrofi_varos;
		  
	  }
	  
	  
	  
	  
	  
	  
	  
	  
	  // telos otan teleiosoun oles oi diadromes 
  	else if(){
		  	printf("H mikroteri diadromi einai %s me varos %d", );
		  	exit(-1); 
    }
  }
}
